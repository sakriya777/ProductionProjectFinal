<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.productionprojectfinal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/Title"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProductionProjectFinal"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.DataBulkAdd"></activity>
        <activity android:name=".Activities.HomeScreen" />
        <activity android:name=".Activities.SignUp" />
        <activity android:name=".Activities.Login" />
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/Title"
            android:theme="@style/Theme.ProductionProjectFinal.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>