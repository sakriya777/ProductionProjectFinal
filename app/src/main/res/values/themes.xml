<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProductionProjectFinal" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/yellow</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">#00000000</item>
        <!-- Customize your theme here. -->
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowTranslucentStatus">true</item>

    </style>

    <style name="NoNotification" parent="Widget.MaterialComponents.MaterialCalendar.Fullscreen">

    </style>

    <style name="Theme.ProductionProjectFinal.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="navigation_theme">
        <item name="colorPrimary">@color/dark_blue</item>
        <item name="colorControlHighlight">@color/yellow</item>
    </style>

    <style name="Theme.ProductionProjectFinal.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ProductionProjectFinal.PopupOverlay" parent="ThemeOverlay.AppCompat" />
</resources>